{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "IT-SUM Library (shared packages)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    "moduleResolution": "Bundler"
  }
}
